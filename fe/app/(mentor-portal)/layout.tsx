import MentorPortalShell from '@/components/layout/MentorPortalShell';

export default function MentorPortalLayout({ children }: { children: React.ReactNode }) {
  return <MentorPortalShell>{children}</MentorPortalShell>;
}
